# A tuple is a sequence of immutable Python object 
# The tuples cannot be changed 

tup1 = ('lam', 'loan', 'bong', 'chit')
tup2 = ('sau', 'tra')
tup3 = ('10',) # need comma when create 1-element-tupble

# Accessing: 
print(tup1[1])

# Updating
# Can't update tuple(because of imutable)
tup4 = tup1+tup2
print(tup4)
